import setting

def percentage_height(percentage):# to set the percentage of the height
    return (setting.height / 100) * percentage

def percentage_widht(percentage):# to set the percentage of the width
    return (setting.widht / 100) * percentage
